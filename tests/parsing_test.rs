extern crate achivit_rs;
#[cfg(test)]
mod tests {
    use achivit_rs::lookup_df::LookupState;
    use color_eyre::Result;
    use std::fmt::Write;
    use achivit_rs::paginate::*;

    #[tokio::test]
    async fn test() -> Result<()> {
        // let list  = get_inn_list_pages()?;
        // dbg!(list);
        Ok(())
    }
}
